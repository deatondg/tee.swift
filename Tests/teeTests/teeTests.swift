    import XCTest
    @testable import tee

    final class teeTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(tee().text, "Hello, World!")
        }
    }
